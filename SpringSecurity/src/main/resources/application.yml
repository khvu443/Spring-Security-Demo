spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;database=Account;encrypt=true;trustServerCertificate=true
    username: sa
    password: binbo123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: sql_server
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
